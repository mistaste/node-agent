name: guardex-node

# Compatibility override retained for existing deployment commands. Stage 1
# transport services are now part of docker-compose.yml and start by default
# on every newly provisioned node.
services: {}
